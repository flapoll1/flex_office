import { ContainsSelectedDatePipe } from './contains-selected-date.pipe';

describe('ContainsSelectedDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ContainsSelectedDatePipe();
    expect(pipe).toBeTruthy();
  });
});
